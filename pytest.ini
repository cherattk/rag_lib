[pytest]
minversion = 9.1.1
addopts = -ra -v --cache-clear --strict-markers --strict-config --color=yes
testpaths =
    tests
pythonpath =
    src
asyncio_mode = auto
markers =
    unit: Unit tests
    integration: Integration tests requiring external services or I/O